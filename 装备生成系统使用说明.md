# 装备生成系统使用说明

## 系统概述

基于鼎炉等级的随机装备生成系统，能够根据玩家当前的鼎炉等级（CauldronLevel）从配置表中获取装备品质生成概率，随机生成装备类型和品质，并根据品质配置生成相应的装备属性。

## 主要特性

✅ **完全随机化**: 装备槽位类型和品质都由服务端随机生成  
✅ **配置驱动**: 基于 EquipQualityConfig 和 FurnaceLevelConfig 配置表  
✅ **鼎炉等级关联**: 根据玩家鼎炉等级决定装备品质概率  
✅ **属性浮动**: 同品质装备具有属性随机浮动  
✅ **槽位差异**: 不同装备槽位有不同的属性分配策略  

## 核心组件

### 1. 装备生成器 (`EquipmentGeneratorComponent`)
- 负责装备的随机生成
- 包含随机数生成器
- 提供槽位类型和品质的随机选择

### 2. 装备临时缓存 (`EquipmentTempCacheComponent`)
- 临时存储生成的装备
- 等待玩家确认或替换
- 支持多玩家并发缓存

### 3. 网络消息协议
- `C2G_GenerateEquipment`: 客户端请求生成装备
- `G2C_GenerateEquipment`: 服务端返回装备信息

## 装备生成逻辑

### 1. 随机选择装备槽位
```csharp
// 9种装备槽位：0-武器, 1-头盔, 2-护甲, 3-腰带, 4-靴子, 5-项链, 6-戒指, 7-护腕, 8-护符
int slotType = Random.Next(0, 9);
```

### 2. 根据鼎炉等级选择品质
```csharp
// 从FurnaceLevelConfig获取概率配置
var furnaceConfig = FurnaceLevelConfigCategory.Instance.Get(cauldronLevel);

// 使用权重随机算法选择品质
int qualityId = SelectQualityByProbability(furnaceConfig);
```

### 3. 生成装备属性
```csharp
// 从EquipQualityConfig获取品质配置
var qualityConfig = EquipQualityConfigCategory.Instance.Get(qualityId);

// 计算属性浮动范围
int minAttr = (int)(baseAttr * (1 - difference));
int maxAttr = (int)(baseAttr * (1 + difference));

// 根据槽位类型分配属性权重
switch (slotType)
{
    case 0: // 武器 - 主要攻击力
        equipment.Attack = Random.Next(minAttr, maxAttr + 1);
        break;
    case 1: case 2: // 头盔、护甲 - 主要防御力
        equipment.Defense = Random.Next(minAttr, maxAttr + 1);
        break;
    // ... 其他槽位
}
```

## 配置表说明

### EquipQualityConfig (装备品质配置)
| 字段 | 说明 | 示例值 |
|------|------|--------|
| Id | 品质ID | 1-15 |
| QualityName | 品质名称 | "凡品", "普通", "精良"... |
| BaseAttr | 基础属性值 | 100, 367, 634... |
| Difference | 属性浮动比例 | 0.3 (±30%) |
| ExtraAttrSlots | 附加属性条数 | 0, 1, 2 |
| RareAffixRate | 稀有词条概率 | 0.0-1.0 |
| Color | 品质颜色代码 | "#7A7A7A", "#B0B0B0"... |

### FurnaceLevelConfig (鼎炉等级配置)
| 字段 | 说明 | 示例值 |
|------|------|--------|
| Id | 配置ID | 1-30 |
| FurnaceLevel | 鼎炉等级 | 1-30 |
| Common | 凡品概率 | 50.0% |
| Basic | 普通概率 | 30.0% |
| Fine | 精良概率 | 14.0% |
| ... | 其他品质概率 | ... |

## 使用方法

### 服务端调用
```csharp
// 获取装备生成器组件
var equipmentGenerator = scene.GetComponent<EquipmentGeneratorComponent>();
if (equipmentGenerator == null)
{
    equipmentGenerator = scene.AddComponent<EquipmentGeneratorComponent>();
}

// 为玩家生成装备
bool success = await equipmentGenerator.GenerateEquipmentForPlayer(playerId);

// 获取生成的装备
var equipmentTempCache = scene.GetComponent<EquipmentTempCacheComponent>();
var equipment = equipmentTempCache.GetTempEquipment(playerId);
```

### 客户端调用
```csharp
// 使用现有的装备生成请求
var request = C2G_GenerateEquipment.Create();
var response = await session.Call(request) as G2C_GenerateEquipment;

if (response.Error == 0)
{
    // 获取生成的装备信息
    var equipment = response.Equipment;
    int slotIndex = response.SlotIndex;
    Debug.Log($"生成装备: {equipment.Name}, 品质: {equipment.Quality}, 颜色: {equipment.Color}, 槽位: {slotIndex}");
}
```

## 测试功能

### 服务端测试
```csharp
// 测试装备生成功能
await EquipmentGeneratorTest.TestEquipmentGeneration(scene);

// 测试概率分布
EquipmentGeneratorTest.TestProbabilityDistribution(scene, cauldronLevel, 1000);

// 测试随机装备生成
await EquipmentGeneratorTest.TestRandomEquipmentGeneration(scene, playerId, 10);
```

### 客户端测试
```csharp
// 客户端使用现有的C2G_GenerateEquipment消息
var request = C2G_GenerateEquipment.Create();
var response = await session.Call(request) as G2C_GenerateEquipment;

if (response.Error == 0)
{
    Log.Info($"装备生成成功: {response.Equipment.Name}");
}
```

## 装备槽位类型

| 槽位ID | 装备类型 | 主属性倾向 |
|--------|----------|------------|
| 0 | 武器 | 攻击力 |
| 1 | 头盔 | 防御力 |
| 2 | 护甲 | 防御力 |
| 3 | 腰带 | 平衡属性 |
| 4 | 靴子 | 平衡属性 |
| 5 | 项链 | 生命值 |
| 6 | 戒指 | 生命值 |
| 7 | 护腕 | 生命值 |
| 8 | 护符 | 生命值 |

## 品质等级

1. 凡品 (Common) - 基础品质
2. 普通 (Basic) - 低级品质
3. 精良 (Fine) - 中等品质
4. 优秀 (Superior) - 较好品质
5. 上品 (Exquisite) - 高品质
6. 稀有 (Rare) - 稀有品质
7. 史诗 (Epic) - 史诗品质
8. 传奇 (Legendary) - 传奇品质
9. 远古 (Ancient) - 远古品质
10. 神话 (Mythic) - 神话品质
11. 仙品 (Celestial) - 仙级品质
12. 圣品 (Holy) - 圣级品质
13. 鸿蒙 (Primordial) - 鸿蒙品质
14. 混元 (Chaotic) - 混元品质
15. 永恒 (Eternal) - 永恒品质

## 扩展说明

### 添加新的装备槽位
1. 修改 `SelectRandomSlotType()` 方法中的随机范围
2. 在 `GenerateEquipmentAttributes()` 方法中添加新槽位的属性分配逻辑
3. 更新装备名称数组

### 修改品质概率
1. 直接修改 `FurnaceLevelConfig.xlsx` 配置表
2. 重新生成配置代码和数据文件
3. 重启服务器生效

### 自定义属性计算
1. 修改 `GenerateEquipmentAttributes()` 方法
2. 添加新的属性计算公式
3. 确保属性值合理性验证

这个系统提供了完整的随机装备生成功能，完全基于配置表驱动，易于调整和扩展。