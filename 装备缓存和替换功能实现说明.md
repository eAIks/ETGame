# 装备缓存和替换功能实现说明

## 功能概述

实现了玩家点击生成装备后的完整流程：
1. 服务端随机生成装备并缓存
2. 客户端显示装备确认界面
3. 玩家可以选择替换装备（保存到数据库）或取消替换（删除缓存）

## 实现的组件和文件

### 1. 数据库实体
- `UserEquipmentInfo.cs` - 装备信息数据库实体，对应表 `ET.Server.User.Equipment.info`
- 主键：`playerId + slotIndex` 的复合键（使用位移操作生成唯一ID）

### 2. 缓存机制
- `EquipmentTempCacheComponent.cs` - 装备临时缓存组件
- `EquipmentTempCacheComponentSystem.cs` - 缓存组件系统，提供缓存操作方法

### 3. 服务端处理器
- `G2M_GenerateEquipmentHandler.cs` - 修改后的装备生成处理器，添加了缓存逻辑
- `G2M_ReplaceEquipmentHandler.cs` - 修改后的装备替换处理器，从缓存获取装备并保存到数据库
- `C2G_CancelTempEquipmentHandler.cs` - 新增：Gate层取消装备处理器
- `G2M_CancelTempEquipmentHandler.cs` - 新增：Map层取消装备处理器

### 4. 客户端逻辑
- `UIEquipmentConfirmComponentSystem.cs` - 修改确认界面的取消按钮，添加取消装备缓存的逻辑

### 5. 协议消息
- `OuterMessage_C_10001.proto` - 添加了 `C2G_CancelTempEquipment` 和 `G2C_CancelTempEquipment` 消息
- `InnerMessage_S_20001.proto` - 添加了 `G2M_CancelTempEquipment` 和 `M2G_CancelTempEquipment` 消息

### 6. 错误码
- `ErrorCode.cs` - 添加了装备临时缓存相关的错误码

## 工作流程

### 装备生成流程
1. 客户端发送 `C2G_GenerateEquipment` 请求
2. Gate转发到Map服务器 `G2M_GenerateEquipment`
3. Map服务器生成随机装备并存入 `EquipmentTempCacheComponent` 缓存
4. 返回装备信息给客户端显示

### 装备替换流程
1. 客户端发送 `C2G_ReplaceEquipment` 请求
2. Gate转发到Map服务器 `G2M_ReplaceEquipment`
3. Map服务器从缓存中获取装备
4. 验证装备信息匹配
5. 更新内存中的装备数据
6. 保存装备到数据库 `ET.Server.User.Equipment.info` 表
7. 清除临时缓存

### 取消装备流程
1. 客户端发送 `C2G_CancelTempEquipment` 请求
2. Gate转发到Map服务器 `G2M_CancelTempEquipment`
3. Map服务器清除对应玩家的临时装备缓存

## 数据库表结构

表名：`ET.Server.User.Equipment.info`

| 字段名 | 类型 | 说明 |
|--------|------|------|
| Id | long | 复合主键：(playerId << 16) + slotIndex |
| PlayerId | long | 玩家ID |
| SlotIndex | int | 装备槽位索引 (0-8) |
| EquipmentId | long | 装备ID |
| Name | string | 装备名称 |
| SlotType | int | 装备类型 |
| Attack | int | 攻击力 |
| Defense | int | 防御力 |
| Health | int | 生命值 |
| Quality | int | 品质 |
| Level | int | 等级 |
| Icon | string | 图标 |
| Description | string | 描述 |
| EquipTime | long | 装备时间 |

## 安全机制

1. **装备验证**：替换装备时验证装备ID和槽位是否匹配缓存
2. **会话验证**：所有请求都验证玩家会话有效性
3. **缓存清理**：成功替换或取消时都会清除临时缓存
4. **错误处理**：完整的错误码和异常处理机制

## 注意事项

1. 由于Tool生成工具存在问题，新增的消息类型需要手动生成对应的C#类
2. 数据库使用MongoDB，通过DBComponent进行操作
3. 缓存组件附加到Map场景上，随场景生命周期管理
4. 复合主键使用位移操作确保唯一性：`(playerId << 16) + slotIndex`